# VD-Trace Agent Notes

## 1. 当前任务 / 需求 / 待办清单

- 当前范围只包含：
  - `tracer` 本体
  - Python GUI / CLI
  - autostart / ctl / 工程化
  - 测试与稳定性
- 不在这里记录逆向结论、密钥、算法判断、脚本推进。
- 当前主线：全面落实项目规范。
  - 当前首轮 `>300` 结构整改已清零：
    - 一方源码 / 头文件 / GUI 页面当前无超 `300` 行残留
    - 后续只做新增回归巡检，不再回退成大单体
  - 当前收尾点：
    - 继续盯 live 主线的 `trace 才崩 / trace 卡顿 / 步数事件停滞`
    - 继续清理重构后可能残留的重复 helper / 失效辅助层
  - 继续清理：
    - 失效辅助层
    - 重复实现
    - 根目录临时产物
    - 旧语义文案残留
  - 当前容量调整：
    - 上游 recorder ring 准备从 `65536` 提升到 `655360`
    - 目标是继续降低极端峰值下的 `dropped_events_total`
    - 本轮仍按原链路验证：
      - 编译
      - GUI / CLI 自测
      - `session_smoke`
      - `smoke_suite`
  - 当前仓库推进：
    - 新建仓库级 `.gitignore`
    - 初始化 git 并绑定 `VehTrace`
    - 只推进主线源码、文档、配置与必要脚本
- 当前 live 主线仍只按 tracer 处理：
  - 继续盯“trace 才崩 / trace 卡顿 / 步数事件停滞”
  - `winhttp` 不回主线
  - `PT` 从主线源码树迁出
- 当前扩展器主线保持：
  - `还原器 -> 扩展器 -> heap peek -> recorder -> file writer`
  - 地址推演和真读内存分开
  - `lea/nop` 不伪造成读内存
  - 重活不回塞 `VEH`
- 常驻检查项：
  - 每轮核心改动后复核 `VEH`
    - 锁竞争
    - 额外内存读取
    - 文本格式化是否回流
  - 每轮交付前都跑：
    - 编译
    - GUI / CLI 自测
    - `session_smoke`
    - `smoke_suite`

## 2. 已解决问题 / 已完成需求

- Python GUI 已成为唯一保留 GUI；旧 C++ GUI 已移除。
- Python CLI 已落地，并与 GUI 共用配置模型与主路径语义。
- 线程策略已收敛到 `屏蔽主线程`，GUI / CLI / `ini` / autostart / smoke 已贯通。
- 过滤器已贯通：
  - `outside / anon / module` 支持 `EDGE / TF`
  - `空转跳出` 已贯通，默认 `32`
- 匿名执行页已脱离旧 `minimal_outside` 路径，能走 rich trace。
- 日志增强已落地：
  - `[ctx]`
  - `heap.peek`
  - `heap.peek.fail`
  - `pass=`
  - 动态范围标记
- `.data/.rdata` 静态引用增强已落地：
  - `[static]`
  - `[static.ptr]`
  - 类型猜测
  - 指向数据预览
  - `*.static_refs.json`
  - `ascii` 后折叠整数 `hex=0x...`
- `recorder worker` 是唯一记录汇编者，`file writer` 是唯一落盘者。
- `PT` 已整体迁到 `backup/pt`：
  - 保留了 `src / docs / tests / tools` 原层级
  - 主线只保留 `DR / TF`
  - 公开 API / C API 已恢复 legacy `PT` 枚举占位，避免旧接入方直接断编
  - GUI / CLI / 配置读取不再静默把旧 `PT` 降级成 `DR`
  - 旧 `PT` 配置现在会在启动时明确报错，指向 `backup/pt`
- `TextFileRecorder` 写盘链路已收口：
  - writer 队列改成阻塞式背压，不再靠批次丢弃保命
  - `kRecorderRingCapacity` 已提升到 `655360`
  - `TraceCaseResult` 现在回传 recorder 侧统计，便于直接验写盘闭环
  - `session_smoke` 新增 `write-accounting` 回归，直接压 recorder 原始事件
- `same-level` 用例的旧式 `<=` 断言已改成当前 `ret` 反汇编断言
- `trigger_wait` / `decrypt_smoke` 已修正日志读取竞态：
  - 先收 recorder，再读日志
  - 避免 suite 内偶发空日志 / 半日志
- 根目录无引用临时脚本与大残留已清掉：
  - `dump.cs`
  - `dump_example.bat`
  - `dump_sym_async.bat`
- 测试 / 自测残留产物已清掉：
  - `bin/release` 下 `VDTrace*.log / *.json / *.state`
  - `VDTraceDecryptSmoke-*` 产物
  - `bin/release/traces/*`
  - `bin/release/dump/traces/*`
  - `obj/GbDump*.dmp`
- 本轮结构整改已完成：
  - `src/VDTraceExtenderAnalyze.cpp` 拆成：
    - 导出壳层
    - `Sim`
    - `Decode`
    - `Memory`
    - `Effects`
  - `src/tests/session_smoke/vdtrace_session_smoke_test.cpp` 拆成：
    - CLI/选项层
    - 配置构造层
    - 基础 trace 用例组
    - 功能用例组
    - 线程/稳定性用例组
  - `src/tests/session_smoke/session_smoke_runtime.cpp` 拆成：
    - 运行时状态/环境层
    - 入口函数层
  - `src/tests/session_smoke/session_smoke_trace.cpp` 拆成：
    - 通用 trace 辅助层
    - 基础运行场景层
    - worker/竞争场景层
  - `src/tools/vdtrace_ctl.cpp` 拆成：
    - 参数解析层
    - configure 命令构造层
    - 主入口层
  - `src/tools/vdtrace_autostart.cpp` 拆成：
    - I/O / 文本输出层
    - 安装/恢复层
    - 主流程层
  - `src/tools/VDTraceControlSupport.cpp` 拆成：
    - 通用路径/线程/pipe 工具层
    - IPC 命令发送层
  - `src/VDTraceFileSinkFormat.cpp` 拆成：
    - 事件壳层
    - 基础格式化 helper
    - 地址/区域标签 helper
    - probe / ctx 块 helper
  - `src/VDTraceStaticRefs.cpp` 拆成：
    - 主块扫描层
    - support / guess / format / export 四层
  - `src/VDTraceProbe.cpp` 拆成：
    - 对外接口层
    - probe parse helper
    - observer / capture helper
  - `src/tools/vdtrace_autostart.cpp` 内未引用的 `PathStartsWithInsensitive` 已移除
  - `src/VDTraceFunctionPreview.cpp` 已拆成壳层 / support / inline
  - `src/VDTraceAsyncSupport.cpp` 已拆成壳层 / util
  - `src/agent/VDTraceAgentMemory.cpp` 已拆成壳层 / support
  - `src/VDTraceThreadContext.cpp` 已拆成壳层 / support
  - `src/VDTraceTriggerCapture.cpp` 已拆成壳层 / support
  - `src/VDTraceDecoder.cpp` 已拆成壳层 / support
  - `src/VDTraceFileSinkWorker.cpp` 已拆成壳层 / process
  - `src/autostart/VDTraceAutoStartHelper.cpp` 已拆成壳层 / support
  - `src/VDTraceHeapPeekSupport.cpp` 已拆成壳层 / detail
  - `src/agent/VDTraceAgentState.cpp` 已补齐 support 层
  - `src/VDTraceRuntime.cpp` 已拆成主流程 / runtime support
  - `src/VDTraceHardware.cpp` 已拆成块分析 / 异常处理
  - `src/VDTraceHardwareTransition.cpp` 已拆成主流程 / transition support
  - `src/VDTraceExtender.cpp` 已拆成壳层 / process
  - `src/VDTraceC.cpp` 已拆成 API 壳层 / C bridge support
  - `src/VDTraceInternal.h` 已把 recorder 声明拆到独立 internal header
  - `src/VDTraceInternalTypes.h` 已把 recorder types 拆到独立 header
  - `src/python_gui/vdtrace_gui/depth_filter_page.py` 已拆出布局模块
  - 根目录 `_codex_*` / `_tmp_*` 临时产物已清掉
- 当前回归结果：
  - `./build_core.bat`
  - `./build_shared.bat full`
  - `./bin/release/vdtrace_session_smoke_test.exe --full-core`
  - `./run_python_gui.bat --self-test`
  - `./run_python_cli.bat self-test`
  - `./bin/release/vdtrace_smoke_suite_test.exe`

## 3. 经验 / 教训 / 高价值信息

- 禁止把“先拆一点”当成交付；拆文件要直接落到职责分层。
- `VEH` 热路径只保留现场强依赖工作：
  - 指令解码
  - 基本块收尾
  - 现场上下文判断
- 适合留在 worker 的工作：
  - 文本格式化
  - 堆值预览
  - 静态引用分析
  - 扩展器附加输出
- `heap.peek` 长度规则必须主事件 / 扩展器 / 函数预览共用一套。
- 静态窗口采样必须走现有 `extender` 主线，不再并行造新组件。
- 根目录禁止继续堆调试临时产物；临时工具要么进 `tools/`，要么做完即删。
- `PT` 既然已移出主线，相关源码可以继续整理，但不允许悄悄重新接回默认构建。
- 现在进一步收口：`PT` 不再留在主线源码树，统一进 `backup` 并保留原层级。
- `smoke_suite` / `session_smoke` / GUI 自测不要并行跑：
  - 它们会抢同一套测试环境
  - 并行失败不能当回归结论
- 写盘回归要区分三层口径：
  - `Session::EventCount()` 不是 recorder 写入数
  - recorder 统计要直接读 `WrittenEventCount()` / `DroppedWriteEventCount()`
  - 真正测 writer 背压要直接压 `TextFileRecorder`，不要借 session 总步数猜
- 读日志类 smoke 要先让 recorder 析构并 flush 完，再做文本断言。
